// Spliced in only by a Windows build. Nothing here is checked by the Linux
// half of the fixture's `cargo build`; what is checked is which matrix
// reaches the file at all.
pub fn windows_only() {}
