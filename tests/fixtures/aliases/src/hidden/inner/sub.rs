//! Named outright by a `pub use inner::sub;` in `hidden`, which is a re-export
//! of a *module*: the corpus's only alias that this phase narrows, and the one
//! shape with two plausible routes to one answer.
