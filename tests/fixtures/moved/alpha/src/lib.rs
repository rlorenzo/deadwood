//! The findings the `moved` baselines record at a file that no longer holds
//! them.
//!
//! `legacy` is private, so what is `pub` inside it is not on this library's
//! public surface and is reported.

mod legacy;
